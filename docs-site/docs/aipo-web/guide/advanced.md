# 고급 기능

## 히스토리 (변경 이력)

### 자동 보관

노트가 수정될 때마다 이전 버전이 자동으로 저장됩니다.

- **보관 기간**: 30일
- **보관 단위**: 언어별 (한국어, 영어, 중국어 각각)
- **만료 알림**: 7일, 1일 전 메일 알림

### 히스토리 보기

1. 노트 보기 화면에서 **시계 아이콘** 클릭
2. 변경 이력 목록 확인
3. 원하는 버전 선택하여 미리보기

### 버전 복원

::: warning 복원 시 주의
버전 복원을 요청하면 AI가 해당 내용으로 노트를 업데이트합니다.
현재 내용은 히스토리에 저장됩니다.
:::

**복원 방법:**
```
"이 노트를 2024-01-15 버전으로 되돌려줘"
```

## 마크다운 내보내기

노트를 마크다운 파일로 내보낼 수 있습니다.

### 내보내기 방법

1. 노트 우측 상단 메뉴 클릭
2. **"마크다운으로 내보내기"** 선택
3. 옵션 선택:
   - 언어 선택 (KO/EN/CN)
   - 댓글 포함 여부

### 내보내기 형식

```markdown
# 회의록: 2024년 1월 마케팅 전략

## 참석자
- 김영희 팀장
- 이철수 과장

## 안건 1: SNS 광고 예산
내용...

---

## Comments (선택 시)

**김영희** (2024-01-20):
> 예산 관련 추가 검토 필요

  **이철수**:
  > 다음 회의에서 논의하겠습니다.
```

## 휴지통 관리

### 휴지통 이동

삭제된 노트는 휴지통으로 이동합니다.
- **30일 보관** 후 자동 영구 삭제
- 보관 기간 내 언제든 복원 가능

### 휴지통 보기

사이드바 하단의 **휴지통** 아이콘 클릭

### 복원

1. 휴지통에서 노트 선택
2. **"복원"** 버튼 클릭
3. 원래 위치로 복원

### 영구 삭제

::: danger 주의
영구 삭제된 노트는 복구할 수 없습니다.
팀 관리자만 영구 삭제할 수 있습니다.
:::

## 다국어 번역

### 자동 번역

노트 작성 시 설정에 따라 다른 언어 버전이 자동 생성됩니다:
- 🇰🇷 **한국어**: 필수 (항상 생성)
- 🇺🇸 **영어**: 선택
- 🇨🇳 **중국어**: 선택

### 번역 재시도

번역이 실패하거나 품질이 낮은 경우:

1. 노트 보기 화면에서 해당 언어 탭 선택
2. **"번역 재시도"** 버튼 클릭
3. AI가 다시 번역 수행

## 설정

### 언어 설정

- **인터페이스 언어**: 한국어/영어/중국어
- **기본 노트 언어**: 노트 보기 시 기본 표시 언어

### 테마 설정

- **라이트 모드**: 밝은 배경
- **다크 모드**: 어두운 배경
- **시스템 설정 따르기**: OS 설정에 연동

### 알림 설정

- **댓글 알림**: 새 댓글 발생 시
- **히스토리 만료 알림**: 7일/1일 전

## 관리자 도구 (Admin)

### Super Admin

환경변수 `DEVELOPERS`에 등록된 사용자만 접근 가능

**기능:**
- 전체 팀 관리
- 모든 공간 접근
- 팀 관리자 지정/해제
- 전체 감사 로그 조회
- 시스템 통계 확인

### Team Admin

Super Admin이 지정한 팀 관리자

**기능:**
- 팀 폴더 구조 Refactoring
- 팀 감사 로그 조회
- 팀 휴지통 영구 삭제

### 감사 로그

모든 활동이 기록됩니다:

| 활동 | 기록 내용 |
|------|----------|
| 로그인/로그아웃 | 시간, IP, User-Agent |
| 노트 생성/수정/삭제 | 노트 ID, 변경 내용 |
| 폴더 생성/삭제 | 폴더 경로 |
| 댓글 작성/삭제 | 댓글 내용 |
| 복원/영구 삭제 | 대상 노트 |
| Refactoring | 변경 전후 구조 |

## API 연동

### API 문서

Swagger UI에서 전체 API를 확인할 수 있습니다:

```
https://a2g.samsungds.net:16002/api-docs
```

### 인증

모든 API 요청에 JWT 토큰이 필요합니다:

```bash
curl -X GET "https://a2g.samsungds.net:16002/api/files/{id}" \
  -H "Authorization: Bearer {your-token}"
```

### 주요 엔드포인트

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | /api/auth/login | 로그인 |
| GET | /api/spaces/:id/tree | 노트 트리 조회 |
| POST | /api/requests/input | 노트 작성 요청 |
| POST | /api/requests/search | 검색 요청 |
| GET | /api/files/:id | 노트 상세 조회 |
