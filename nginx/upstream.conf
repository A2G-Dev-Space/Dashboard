# Auto-generated by deploy.sh (active: blue)
# Initial default — deploy.sh will overwrite on each deploy

upstream api {
    server api-blue:3000;
    keepalive 256;
    keepalive_requests 1000;
    keepalive_timeout 60s;
}

upstream dashboard {
    server dashboard-blue:80;
}
